{
  "name": "@devicons/api",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "echo \"API boundary ready — implementation comes after the SVG generator\"",
    "build": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "latest"
  }
}