# DevIcons API

Reserved API boundary.

Target endpoint:

`GET /icons?i=html,css,js&theme=dark&size=48&perline=5&gap=12&style=minimal`

The API should remain a thin layer around the shared icon registry and SVG generator.
